const UserAverageSessions = ({ userData }) => {
	//console.log("userAverageSessions:", userAverageSessions);

	return (
		<div>
			<p>Durée moyenne des sessions</p>
		</div>
	);
};

export default UserAverageSessions;
